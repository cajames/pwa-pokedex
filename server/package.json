{
  "name": "offline-server",
  "version": "1.0.0",
  "description": "Offline app demo server",
  "main": "index.js",
  "scripts": {
    "start": "json-server db.js",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "author": "Chris James",
  "license": "ISC",
  "dependencies": {
    "json-server": "^0.12.1"
  }
}
